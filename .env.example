# PostgreSQL (docker-compose)
POSTGRES_USER=aviso
POSTGRES_PASSWORD=aviso_dev_password
POSTGRES_DB=aviso

# Prisma
DATABASE_URL=postgresql://aviso:aviso_dev_password@localhost:5432/aviso

# Auth.js — generate AUTH_SECRET with: npx auth secret
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
